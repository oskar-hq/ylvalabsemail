services:
  mailer:
    build: .
    container_name: ylva-mailer
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - mailer-data:/data

volumes:
  mailer-data:
